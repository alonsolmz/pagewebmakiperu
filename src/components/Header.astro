---
const navItems = [
  { name: 'Inicio', href: '#' },
  { name: 'Nosotros', href: '#nosotros' },
  { name: 'Catálogo', href: '#grid-vehiculos' },
];
---

<header class="bg-white/95 backdrop-blur-md border-b border-gray-100 sticky top-0 z-50">
  <div class="max-w-[1400px] mx-auto px-4 md:px-6 h-20 flex items-center justify-between">
    
    <a href="#" class="flex items-center">
      <div class="bg-[#0B132B] px-4 py-2 rounded-xl flex items-center shadow-md">
        <span class="text-xl md:text-2xl font-black text-white tracking-tighter">MAQUI</span>
        <span class="text-[#F2B705] text-xl md:text-2xl font-black tracking-tighter ml-1">PERU</span>
      </div>
    </a>

    <nav class="flex items-center gap-4 md:gap-8">
      {navItems.map(item => (
        <a href={item.href} class="text-[9px] md:text-[10px] font-black text-slate-600 hover:text-[#F2B705] transition-colors uppercase tracking-[0.2em]">
          {item.name}
        </a>
      ))}
    </nav>
  </div>
</header>