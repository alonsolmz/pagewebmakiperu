---
const navItems = [
  { name: 'Inicio', href: '#' },
  { name: 'Nosotros', href: '#nosotros' },
  { name: 'Catálogo', href: '#grid-vehiculos' },
];
---

<header class="bg-white/95 backdrop-blur-md border-b border-gray-200 sticky top-0 z-50 shadow-sm">
  <div class="max-w-[1400px] mx-auto px-4 md:px-6 h-20 flex items-center justify-between">
    
    <a href="#" class="flex items-center group">
      <div class="bg-[#0B132B] px-5 py-2.5 rounded-[14px] flex items-center shadow-lg group-hover:shadow-xl group-hover:scale-105 transition-all duration-300">
        <span class="text-xl md:text-2xl font-black text-white tracking-tighter">MAKI</span>
        <span class="text-[#F2B705] text-xl md:text-2xl font-black tracking-tighter ml-1">PERU</span>
      </div>
    </a>

    <nav class="flex items-center gap-6 md:gap-10">
      {navItems.map(item => (
        <a href={item.href} class="text-[9px] md:text-[10px] font-black text-slate-600 hover:text-[#F2B705] relative group transition-colors uppercase tracking-[0.2em]">
          {item.name}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-[#F2B705] group-hover:w-full transition-all duration-300"></span>
        </a>
      ))}
    </nav>
  </div>
</header>