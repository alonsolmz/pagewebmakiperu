---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase';
import '../styles/global.css';
import { Image } from 'astro:assets';
import heroImg from '../assets/imagenportadamaqui.png'; 

const { data: vehiculos, error } = await supabase
  .from('vehiculos')
  .select('*')
  .eq('visible', true)
  .order('created_at', { ascending: false });

if (error) console.error(error);
---

<Layout title="MakiPeru | Maquinaria Pesada">
  <main class="bg-[#F5F6F8] min-h-screen">
    
    <div class="px-4 md:px-10 py-6 md:py-10">
      <div class="relative overflow-hidden shadow-2xl bg-[#0B132B] rounded-[40px] h-[500px] md:h-[650px] group">
        <Image src={heroImg} alt="MakiPeru" class="absolute inset-0 w-full h-full object-cover opacity-70 group-hover:opacity-80 transition-opacity duration-500" />
        <div class="absolute inset-0 bg-gradient-to-t from-[#0B132B] from-0% via-[#0B132B]/30 via-50% to-transparent flex items-center px-6 md:px-20">
          <div class="max-w-3xl">
            <h1 class="text-5xl md:text-8xl font-black text-white italic tracking-tighter leading-[0.9] uppercase drop-shadow-lg">
              POTENCIA y <br/> <span class="text-[#F2B705] drop-shadow-lg">MOVIMIENTO</span>
            </h1>
            <p class="text-lg md:text-2xl text-white/95 font-light italic border-l-4 border-[#F2B705] pl-6 mt-8 drop-shadow-md">
              Líderes en suministro de maquinaria para minería y construcción civil.
            </p>
          </div>
        </div>
      </div>
    </div>

    <section id="nosotros" class="py-20 md:py-32 px-6">
      <div class="max-w-6xl mx-auto">
        <div class="bg-gradient-to-br from-white to-gray-50 p-8 md:p-20 rounded-[50px] shadow-lg border border-gray-100">
          <div class="grid md:grid-cols-3 gap-12 md:gap-16 items-start">
            <div class="md:col-span-1">
              <h2 class="text-3xl md:text-4xl font-black text-[#0B132B] uppercase tracking-tighter leading-tight">
                MÁS QUE <br/><span class="text-[#F2B705]">MÁQUINAS</span>
              </h2>
              <div class="flex gap-2 mt-6">
                <div class="w-16 h-1.5 bg-[#F2B705] rounded-full"></div>
                <div class="w-10 h-1.5 bg-[#F2B705]/40 rounded-full"></div>
              </div>
            </div>
            <div class="md:col-span-2">
              <div class="space-y-6">
                <p class="text-lg font-bold text-[#0B132B] leading-relaxed">
                  En MakiPeru, entendemos que cada proyecto es un compromiso con el desarrollo del país.
                </p>
                <p class="text-base md:text-lg text-slate-600 leading-relaxed">
                  Nuestra trayectoria se basa en la selección rigurosa de equipos de alto rendimiento. No solo vendemos maquinaria; entregamos herramientas de confianza que garantizan la continuidad operativa en los terrenos más exigentes.
                </p>
              </div>
              <div class="grid grid-cols-2 gap-4 mt-12">
                <div class="text-center">
                  <div class="text-3xl font-black text-[#F2B705] mb-2">50+</div>
                  <p class="text-xs text-slate-600 font-semibold uppercase">Máquinas</p>
                </div>
                <div class="text-center">
                  <div class="text-3xl font-black text-[#F2B705] mb-2">24/7</div>
                  <p class="text-xs text-slate-600 font-semibold uppercase">Apoyo</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="grid-vehiculos" class="px-6 md:px-20 pb-24 space-y-12">
      <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-6 max-w-7xl mx-auto">
        <div class="w-full">
          <h2 class="text-3xl md:text-4xl font-black text-[#0B132B] uppercase tracking-tighter mb-6">
            Nuestro <span class="text-[#F2B705]">Catálogo</span>
          </h2>
          <div class="flex gap-3 overflow-x-auto w-full pb-2 no-scrollbar">
            <button class="filter-btn active bg-[#0B132B] text-white px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest whitespace-nowrap transition-all duration-300 shadow-md">Todos</button>
            <button class="filter-btn bg-white text-slate-600 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-200 whitespace-nowrap hover:border-[#F2B705] transition-all duration-300" data-category="Linea Amarilla">Línea Amarilla</button>
            <button class="filter-btn bg-white text-slate-600 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-200 whitespace-nowrap hover:border-[#F2B705] transition-all duration-300" data-category="Volquetes">Volquetes</button>
            <button class="filter-btn bg-white text-slate-600 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-200 whitespace-nowrap hover:border-[#F2B705] transition-all duration-300" data-category="Mineria">Minería</button>
          </div>
        </div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto" id="vehicles-grid">
        {vehiculos?.map((item) => (
          <div class="vehiculo-card bg-white overflow-hidden transition-all duration-300 flex flex-col h-full hover:shadow-2xl hover:-translate-y-2 group" 
               style="border-radius: 32px; box-shadow: 0 4px 15px rgba(11, 19, 43, 0.08);" data-category={item.categoria} data-price={item.precio || 0}>
            <div class="relative aspect-[4/3] overflow-hidden bg-gradient-to-br from-slate-200 to-slate-100">
              <img src={item.imagen_url} alt={item.nombre} class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300" loading="lazy" />
              <div class="absolute top-4 right-4 bg-[#0B132B] text-white text-[8px] font-black px-4 py-2 rounded-full uppercase tracking-widest shadow-lg">
                {item.categoria}
              </div>
            </div>
            <div class="p-8 text-center flex flex-col flex-1">
              <h3 class="text-lg md:text-xl font-black text-[#0B132B] mb-3 uppercase italic tracking-tighter leading-tight group-hover:text-[#F2B705] transition-colors duration-300">
                {item.nombre}
              </h3>
              <p class="text-[#F2B705] font-black text-2xl mb-8">
                {item.precio ? `$${Number(item.precio).toLocaleString()} USD` : 'CONSULTAR'}
              </p>
              <div class="mt-auto">
                <a href={`https://wa.me/51968348172?text=Deseo información sobre: ${item.nombre}`} 
                   target="_blank"
                   class="bg-[#0B132B] text-white block w-full py-4 px-4 rounded-2xl text-[10px] font-black uppercase tracking-widest hover:bg-[#F2B705] hover:text-[#0B132B] transition-all duration-300 active:scale-95">
                  Cotizar WhatsApp
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <a 
      href="https://wa.me/51968348172" 
      target="_blank"
      class="fixed bottom-8 right-8 z-[100] bg-[#0B132B] text-white px-7 py-4 rounded-full font-black text-[11px] uppercase tracking-[0.2em] shadow-[0_10px_40px_rgba(11,19,43,0.4)] hover:scale-125 hover:bg-[#F2B705] hover:text-[#0B132B] hover:shadow-[0_15px_50px_rgba(242,183,5,0.4)] transition-all duration-300 active:scale-95 flex items-center gap-3"
    >
      <span class="relative flex h-3 w-3">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[#F2B705] opacity-75"></span>
        <span class="relative inline-flex rounded-full h-3 w-3 bg-[#F2B705]"></span>
      </span>
      CONTACTANOS
    </a>

  </main>
</Layout>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('.filter-btn');
    const cards = document.querySelectorAll('.vehiculo-card');

    buttons.forEach(btn => {
      btn.addEventListener('click', () => {
        const category = btn.getAttribute('data-category') || 'Todos';
        
        buttons.forEach(b => {
          b.classList.remove('active', 'bg-[#0B132B]', 'text-white');
          b.classList.add('bg-white', 'text-slate-400');
        });
        btn.classList.add('active', 'bg-[#0B132B]', 'text-white');
        btn.classList.remove('bg-white', 'text-slate-400');

        cards.forEach(card => {
          const cardCat = card.getAttribute('data-category');
          if (category === 'Todos' || cardCat === category) {
            card.style.display = 'flex';
          } else {
            card.style.display = 'none';
          }
        });
      });
    });
  });
</script>