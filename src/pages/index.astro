---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabase';
import '../styles/global.css';
import { Image } from 'astro:assets';
import heroImg from '../assets/imagenportadamaqui.png'; 

const { data: vehiculos, error } = await supabase
  .from('vehiculos')
  .select('*')
  .eq('visible', true)
  .order('created_at', { ascending: false });

if (error) console.error(error);
---

<Layout title="MaquiPeru | Maquinaria Pesada">
  <main class="bg-[#F5F6F8] min-h-screen">
    
    <div class="px-4 md:px-10 py-6 md:py-10">
      <div class="relative overflow-hidden shadow-2xl bg-[#0B132B] rounded-[40px] h-[500px] md:h-[650px]">
        <Image src={heroImg} alt="MaquiPeru" class="absolute inset-0 w-full h-full object-cover opacity-60" />
        <div class="absolute inset-0 bg-gradient-to-t from-[#0B132B] via-transparent flex items-center px-6 md:px-20">
          <div class="max-w-3xl">
            <h1 class="text-5xl md:text-8xl font-black text-white italic tracking-tighter leading-[0.9] uppercase">
              POTENCIA y <br/> <span class="text-[#F2B705]">MOVIMIENTO</span>
            </h1>
            <p class="text-lg md:text-2xl text-white/90 font-light italic border-l-4 border-[#F2B705] pl-6 mt-8">
              Líderes en suministro de maquinaria para minería y construcción civil.
            </p>
          </div>
        </div>
      </div>
    </div>

    <section id="nosotros" class="py-24 px-6">
      <div class="max-w-5xl mx-auto bg-white p-10 md:p-20 rounded-[50px] shadow-sm border border-gray-100">
        <div class="grid md:grid-cols-3 gap-12 items-center">
          <div class="md:col-span-1">
             <h2 class="text-3xl font-black text-[#0B132B] uppercase tracking-tighter leading-none">
              MÁS QUE <br/><span class="text-[#F2B705]">MÁQUINAS</span>
            </h2>
            <div class="w-12 h-2 bg-[#F2B705] mt-4"></div>
          </div>
          <div class="md:col-span-2 text-slate-600 text-lg leading-relaxed space-y-6">
            <p class="font-bold text-[#0B132B]">
              En MaquiPeru, entendemos que cada proyecto es un compromiso con el desarrollo del país. 
            </p>
            <p>
              Nuestra trayectoria se basa en la selección rigurosa de equipos de alto rendimiento. No solo vendemos maquinaria; entregamos herramientas de confianza que garantizan la continuidad operativa en los terrenos más exigentes.
            </p>
          </div>
        </div>
      </div>
    </section>

    <section id="grid-vehiculos" class="px-6 md:px-20 pb-24 space-y-12">
      <div class="flex flex-col md:flex-row justify-between items-center gap-6 max-w-7xl mx-auto">
        <div class="flex gap-2 overflow-x-auto w-full md:w-auto pb-2 no-scrollbar">
          <button class="filter-btn active bg-[#0B132B] text-white px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest">Todos</button>
          <button class="filter-btn bg-white text-slate-400 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-100" data-category="Linea Amarilla">Línea Amarilla</button>
          <button class="filter-btn bg-white text-slate-400 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-100" data-category="Volquetes">Volquetes</button>
          <button class="filter-btn bg-white text-slate-400 px-6 py-3 rounded-xl font-black text-[9px] uppercase tracking-widest border border-gray-100" data-category="Mineria">Minería</button>
        </div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto" id="vehicles-grid">
        {vehiculos?.map((item) => (
          <div class="vehiculo-card bg-white overflow-hidden shadow-md transition-all flex flex-col h-full" 
               style="border-radius: 32px;" data-category={item.categoria} data-price={item.precio || 0}>
            <div class="relative aspect-[4/3] overflow-hidden">
              <img src={item.imagen_url} alt={item.nombre} class="w-full h-full object-cover" loading="lazy" />
              <div class="absolute top-4 right-4 bg-[#0B132B] text-white text-[8px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest">
                {item.categoria}
              </div>
            </div>
            <div class="p-8 text-center flex flex-col flex-1">
              <h3 class="text-xl font-black text-[#0B132B] mb-2 uppercase italic tracking-tighter">
                {item.nombre}
              </h3>
              <p class="text-[#F2B705] font-black text-2xl mb-8">
                {item.precio ? `$${Number(item.precio).toLocaleString()} USD` : 'CONSULTAR'}
              </p>
              <div class="mt-auto">
                <a href={`https://wa.me/51968348172?text=Deseo información sobre: ${item.nombre}`} 
                   target="_blank"
                   class="bg-[#0B132B] text-white block w-full py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest hover:bg-[#F2B705] hover:text-[#0B132B] transition-all">
                  Cotizar WhatsApp
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <a 
      href="https://wa.me/51968348172" 
      target="_blank"
      class="fixed bottom-8 right-8 z-[100] bg-[#0B132B] text-white px-7 py-4 rounded-full font-black text-[11px] uppercase tracking-[0.2em] shadow-[0_10px_30px_rgba(11,19,43,0.3)] hover:scale-110 hover:bg-[#F2B705] hover:text-[#0B132B] transition-all active:scale-95 flex items-center gap-3"
    >
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[#F2B705] opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-[#F2B705]"></span>
      </span>
      CONTACTANOS
    </a>

  </main>
</Layout>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const buttons = document.querySelectorAll('.filter-btn');
    const cards = document.querySelectorAll('.vehiculo-card');

    buttons.forEach(btn => {
      btn.addEventListener('click', () => {
        const category = btn.getAttribute('data-category') || 'Todos';
        
        buttons.forEach(b => {
          b.classList.remove('active', 'bg-[#0B132B]', 'text-white');
          b.classList.add('bg-white', 'text-slate-400');
        });
        btn.classList.add('active', 'bg-[#0B132B]', 'text-white');
        btn.classList.remove('bg-white', 'text-slate-400');

        cards.forEach(card => {
          const cardCat = card.getAttribute('data-category');
          if (category === 'Todos' || cardCat === category) {
            card.style.display = 'flex';
          } else {
            card.style.display = 'none';
          }
        });
      });
    });
  });
</script>